<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>neophob.com</groupId>
	<artifactId>PixelController</artifactId>
	<name>PixelController</name>
	<packaging>jar</packaging>	
	<version>1.0-SNAPSHOT</version>
	<description>PixelController Driver</description>
	<prerequisites>
		<maven>2.2.1</maven>
	</prerequisites>
	
	<properties>
		<sonar.dynamicAnalysis>false</sonar.dynamicAnalysis>
		<sonar.phase>generate-sources</sonar.phase>
		
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>		
	</properties>	
	
	<developers>
		<developer>
			<name>Michael Vogt</name>
			<id>michu</id>
			<email>michu@neophob.com</email>
		</developer>
	</developers>
	
	<repositories>
		<repository>
  			<id>maven2-repository.java.net</id>
  			<name>Java.net Repository for Maven</name>
  			<url>http://download.java.net/maven/2/</url>
  			<layout>default</layout>
		</repository>
  	</repositories>
  
	<pluginRepositories>
		<pluginRepository>
			<id>mc-release</id>
			<name>Local Maven repository of releases</name>
			<url>http://mc-repo.googlecode.com/svn/maven2/releases</url>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
			<releases>
				<enabled>true</enabled>
			</releases>
		</pluginRepository>
	</pluginRepositories>

	<build>
 		<plugins>
		
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>2.3.2</version>
				<configuration>
					<source>1.6</source>
					<target>1.6</target>
				</configuration>
			</plugin>
			
			<!-- Package all files together -->
			<plugin>
				<artifactId>maven-assembly-plugin</artifactId>
  				<version>2.2.1</version>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>assembly</goal>
						</goals>
						<configuration>
							<finalName>
 								${project.artifactId}-${project.version}
							</finalName>
							<!-- 
								<appendAssembly>false</appendAssembly>
							-->
							<outputDirectory>
								${project.build.directory}
							</outputDirectory>
							<descriptors>
								<descriptor>
									${basedir}/src/build/assembly.xml
								</descriptor>
							</descriptors>
						</configuration>
					</execution>
				</executions>
			</plugin>
			
			
			<plugin>
				<groupId>com.mycila.maven-license-plugin</groupId>
				<artifactId>maven-license-plugin</artifactId>
				<version>1.9.0</version>
				<configuration>
					<header>src/build/header.txt</header>
				</configuration>
			</plugin>
			
			<!-- Build classpath strings to insert into scripts -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
 				<version>2.3</version>
				<executions>
					<execution>
						<id>build-classpath-win</id>
						<phase>generate-resources</phase>
						<goals>
							<goal>build-classpath</goal>
						</goals>
						<configuration>
							<prefix>lib</prefix>
							<fileSeparator>\</fileSeparator>
							<pathSeparator>;</pathSeparator>
							<outputFilterFile>true</outputFilterFile>
							<outputFile>
								${project.build.directory}/${project.artifactId}-${project.version}-distribution/${project.artifactId}-${project.version}/classpath-win.properties
							</outputFile>
							<excludeGroupIds>
								junit
							</excludeGroupIds>
						</configuration>
					</execution>
  
					<execution>
						<id>build-classpath-unix</id>
						<phase>generate-resources</phase>
						<goals>
							<goal>build-classpath</goal>
						</goals>
						<configuration>
							<prefix>lib</prefix>
							<fileSeparator>/</fileSeparator>
							<pathSeparator>:</pathSeparator>
							<outputFilterFile>true</outputFilterFile>
							<outputFile>
								${project.build.directory}/${project.artifactId}-${project.version}-distribution/${project.artifactId}-${project.version}/classpath-unix.properties
							</outputFile>
							<excludeGroupIds>
								junit
							</excludeGroupIds>						
						</configuration>
					</execution>
				
				</executions>
			</plugin>			
			
			<plugin>
				<!-- The Surefire Plugin is used during the test phase of the build lifecycle to 
				execute the unit tests of an application. It generates reports in 2 different file 
				formats -->
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>2.9</version>
			</plugin>
			
		</plugins>
	</build>
	
    <dependencies>
    
    <!-- dependencies from repos -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.8.2</version>
            <scope>test</scope>
        </dependency>
        
   		<dependency>
			<groupId>commons-collections</groupId>
			<artifactId>commons-collections</artifactId>
			<version>3.2.1</version>
		</dependency>
        
   		<dependency>
			<groupId>commons-lang</groupId>
			<artifactId>commons-lang</artifactId>
			<version>2.5</version>
		</dependency>

		<dependency>
    		<groupId>org.eclipse.jdt.core.compiler</groupId>
    		<artifactId>ecj</artifactId>
    		<version>3.5.1</version>
		</dependency>

   		<dependency>
			<groupId>net.java.dev.jna</groupId>
			<artifactId>jna</artifactId>
			<version>3.2.7</version>
		</dependency>

		<!-- local deps, make sure you installed them in your local repo! -->
		
		<!-- processing -->
   		<dependency>
			<groupId>org.processing</groupId>
			<artifactId>core</artifactId>
			<version>1.2.1</version>
		</dependency>

   		<dependency>
			<groupId>org.processing</groupId>
			<artifactId>net</artifactId>
			<version>1.2.1</version>
		</dependency>

   		<dependency>
			<groupId>org.processing</groupId>
			<artifactId>pde</artifactId>
			<version>1.2.1</version>
		</dependency>

   		<dependency>
			<groupId>org.processing</groupId>
			<artifactId>serial</artifactId>
			<version>1.2.1</version>
		</dependency>

		<!-- processing deps -->
   		<dependency>
			<groupId>org.qbang</groupId>
			<artifactId>rxtx</artifactId>
			<version>2.2pre5</version>
		</dependency>

   		<dependency>
			<groupId>compartmental.net</groupId>
			<artifactId>minim</artifactId>
			<version>2.0.2</version>
		</dependency>
		
   		<dependency>
			<groupId>compartmental.net</groupId>
			<artifactId>jsminim</artifactId>
			<version>2.0.2</version>
		</dependency>

   		<dependency>
			<groupId>compartmental.net</groupId>
			<artifactId>minim-spi</artifactId>
			<version>2.0.2</version>
		</dependency>

   		<dependency>
			<groupId>compartmental.net</groupId>
			<artifactId>tritonus_aos</artifactId>
			<version>2.0.2</version>
		</dependency>

   		<dependency>
			<groupId>compartmental.net</groupId>
			<artifactId>tritonus_share</artifactId>
			<version>2.0.2</version>
		</dependency>

		<!-- src: http://code.google.com/p/neorainbowduino/ -->
   		<dependency>
			<groupId>com.neophob</groupId>
			<artifactId>neorainbowduino</artifactId>
			<version>0.82</version>
		</dependency>

		<!-- src: https://github.com/neophob/processing-blinkenlights -->
   		<dependency>
			<groupId>com.neophob</groupId>
			<artifactId>processing-blinkenlight</artifactId>
			<version>0.5</version>
		</dependency>
        
    </dependencies>
</project>